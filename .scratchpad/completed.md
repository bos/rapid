# Completed

Reverse-chronological one-liners. Details live in git history — these are just breadcrumbs.

- **HTML injection hardening** (Jul 2026) — Ported upstream XSS fixes, added strict DOMPurify sanitization and safe URL handling across external/metadata HTML, then added a hash-based CSP and a DOMPurify-backed Trusted Types default policy with restricted dynamic script URLs. CSP hashes are checked in CI and refreshed after deploy-time HTML rewriting.
- **`TreeStore` + SettingsSystem/LocalizationSystem** (Jul 2026) — New `modules/lib/TreeStore.ts`: generic nested-tree store (path `get`/`peek`/`set`/`has`/`unset`, `toFlat`/`fromFlat` flat-key codec, `merge`, static `parsePath`); `modules/util/keypath.ts` folded in and deleted. `SettingsSystem` now holds a `TreeStore` and only adds the `rapid.settings.*` namespace + legacy migrations. `LocalizationSystem` `_strings`→`_store` (a `TreeStore`, keyed `[locale][resource][…]`), `_scopes`→`_resources`, hot reads via `peek` (copy-free). Retired `<TX_DOT>`: producers percent-encode dots as `%2E`, l10n normalizes legacy `<TX_DOT>` data keys to dots at load. 3260 unit / 121 browser pass.
- **SettingsSystem design doc** (Jul 2026) — Added `.github/design/settings-system.md` proposing a typed, versioned settings model with validation+migrations, adapter-based persistence (local + optional OSM sync), legacy key import mapping, and phased rollout focused first on imagery/settings recents.
- **RBush → SpatialSystem consolidation** (Jun 2026) — Removed all remaining standalone `new RBush` instances outside `SpatialSystem` itself: `PixiLayerLabels` (label+debug placement caches → `LabelItem extends SpatialItem` with `contents: LabelContents`, two spatial caches `'labels'` / `'labels-debug'`), `NominatimService` (`_nominatimCache` → `'nominatim'` cache), `ValidationCache` (`recheckRBush+recheckBoxes` → `'validation-base'`/`'validation-head'`, `context` threaded into constructor), `VectorTileService` (per-source×zoom rbush → `'vt-<id>-z<zoom>'` caches). No more direct RBush imports outside the spatial module.
- **PixiLayerLabels rope-label squish fix** (Jun 2026) — Reverted rope box sampling back from `geomCoverageBoxes` to `geomLineSegments` (the original uniform arc-length sampler). `geomCoverageBoxes` subdivides per-segment (not uniformly spaced), which broke the `scaleX` math and produced squished text on roads made of many short segments. `geomLineSegments` is the right tool for rope placement; `geomCoverageBoxes` is reserved for conflation coverage.

- **NetworkSystem `_completed` Map + FetchEnvelope everywhere** (Jun 2026) — `completed: Set` → `protected _completed: Map<RequestID, number>`. Status is real HTTP code or `STATUS_SKIPPED`/`STATUS_ERROR` sentinel. NetworkSystem is single writer; public API: `isCompleted`, `getStatus`, `markCompleted`, `forget`. Only requests with explicit `requestID` are recorded (auto-computed IDs excluded). `FetchEnvelope<T>` replaces bespoke `OsmFetchResult` — all worker listeners now return envelopes; main-thread path uses `fetchEnvelope()` helper. New `fetchEnvelope()` public method on NetworkSystem for callers needing status-branch-without-throw (OsmService). `FetchError` extended to accept `FetchErrorInit` fields (reconstructable from envelope). Services: don't-retry services shed manual `completed.add`; do-retry services (Streetside, MapWithAI, OsmService) use `network.forget(id)`. Blocked-region tiles use `markCompleted`. OsmService `loadNotes` fixed to use `_noteZoom` not `_tileZoom`. Stuck-note-tile retry bug fixed.
- **SpatialSystem/NetworkSystem refactor + test suite repair** (Jun 2026) — Flat SpatialSystem (one RBush per `spatialID`). Fixed `replaceItems` not populating `cache.items` Map (broke remove + hasItem/getItem). Fixed `replaceData` storing `this`/`this.id` instead of `d`/`d.id`. Added `hasItemAtLoc()` WGS84 helper. Fixed `EditSystem._reset` to `clearMatching(id => id.startsWith('editor_'))` (clears segment caches too). Fixed `clearMatching` iterating `_completed.keys()` not `_completed`. Fixed `OsmoseService.getData` mismatched cache ID. Fixed `address.js` hardcoded `'osm-staging'` (hyphen → `editor.spatialIDForGraph()`). Fixed `loadNotes` zoom. All service tests fixed for new tile-tracking pattern (`network.isCompleted` instead of `spatial.hasItemAtLoc` for tile-load checks). NetworkSystem test suite updated for Map API. 3170 unit tests / 121 browser tests pass.
- **Remove Legacy `SchedulerSystem` wrappers** (Jun 2026) - `scheduleTimeout`/`scheduleInterval` retained for backward compat, they have zero external callers
- **DashLine performance backlog cleanup** (Jun 2026) — Removed the stale `DashLine performance` item from `backlog.md` after confirming the texture-based path and its fixes were already merged; the completed notes now match the current implementation.
- **`actionMove` world-space rewrite, JSDoc, and U-shape bug fix** (May 2026) — Removed `viewport` parameter; all geometry now computed in world coordinates with a local origin (matching `actionRotate`, `actionReflect`, `actionScale`, `actionOrthogonalize`, `actionCircularize`). `limitDelta` uses `vecProject` for edge picking. Added `nodeOriginalLocal` helper used in `replaceMovedVertex`'s angle test to fix a bug where moving a U-shaped driveway connected to a straight road at both endpoints inserted spurious shape-preserving vertices (root cause: angle test was reading post-move positions of shared endpoints). Full JSDoc on all 13 internal functions. 12 tests including a U-shape regression. Call sites updated: `MoveMode.ts`, `SelectOsmMode.ts`, `PasteBehavior.ts`, `paste.js`.
- **JSDoc duplicate-title cleanup** (May 2026) — Removed 30 redundant first-line JSDoc titles that only repeated the documented class name, fixing IntelliSense noise in services, modes, and lib classes.
- **Pixi atlas direct source uploads + tile-only edge padding** (May 2026) — `AtlasAllocator` stores `AtlasItemSource` (ImageData | HTMLCanvasElement | HTMLImageElement | ImageBitmap) and uploads directly via 7-arg `texSubImage2D` (WebGL) / `copyExternalImageToTexture` (WebGPU). JS `_getItemPixels` padding loop deleted. `padded` flag on `AtlasItem` distinguishes tile sources (pre-padded via `_fromEdgePaddedCanvas` two-pass drawImage) from symbol/text/icon sources (1px transparent ring, no replication needed). `PixiLayerBackgroundTiles` uses `fetch` + `createImageBitmap` for off-thread decode. Closes #1650.
- **Pixi label rasterization + managed feature lifecycle** (May 2026) — Decoupled `PixiLayerLabels` texture creation from `render()` to fix re-entrant renderer corruption. New `PixiFeatureLabel` managed-feature class follows the canonical layer/feature pattern. Label textures queue and drain via `SchedulerSystem` outside the render loop.
- **render_worldcoord Step 5: delete PixiGeometryPart** (May 2026) — Removed `PixiGeometryPart.ts` (210 lines) and all legacy screen-coord rendering paths. `AbstractPixiFeature` drops dual-path scaffolding; replaces public `geom` + `setCoords()` with `_geom`/`_geomDirty` and a `geom`/`geometry` getter pair. Each of Line/Point/Polygon collapses `update()` + `updateWorld()` into a single world-coord `update()`. `PixiLayerOsm` small-feature cull simplified to use `entity.geoms.world.extent`. Net: ~1004 lines deleted (`f12bb91e1`). Branch `render_worldcoord` migration complete.
- **DashLine halo fixes** (May 2026) — `PixiLayerMapUI.reset()` iterates a children snapshot to avoid splice-during-iteration. `AbstractPixiFeature.destroy()` uses `halo.destroy({ children: true })`. Line/Polygon halos get `scale: localScale` in `HALO_STYLE` so DashLine converts world-unit widths correctly. Root cause for dashes appearing wrong: (1) `DashLine.strokeStyle.textureSpace` was effectively `'local'` for a while — that branch stretches the texture across each segment's bounding box, making longer segments show longer dashes. Reverted to `'global'`. (2) The pattern texture canvas is pow2-padded for WebGL1 REPEAT-wrap support, but under `'global'` with a uniform matrix scale that made the cycle length wrong by `nextPow2(dashSize)/dashSize`. Fixed by storing the texture's pow2 dimensions on the DashLine instance and non-uniformly scaling the per-segment matrix by `(dashSize / texW, width / texH)`. Also reordered matrix composition to scale-then-rotate (non-uniform scale requires this to keep the texture-x axis aligned with the line direction).
- **render_worldcoord Steps 3+4 complete** (May 2026) — Step 3 (`bdfa56684`): migrated `basemap`, `editblocks`, `debug`, `map-ui` groups and all remaining `setCoords` callers to world-coord path. Step 4 (`b3f5c4417`): `PixiLayerLabels` ported to world-coord geometry — label positions in world space, rope label coords world-local, zoom/rotation invalidation guard retained, `return; // not yet` guard removed.
- **Pattern texture fixes + line markers world-coord port** (May 2026) — `PixiFeaturePolygon` pattern textures now tile correctly at all zooms (fixed UV scale). Line marker rendering ported to world-coord path (`cd7142610`, `6acff526e`, `f594fd8f7`).
- **GeometryPart `.local` cache implementation** (May 2026) — Added `GeometryPartLocalData` interface to store coordinates relative to world origin (small numbers suitable for stable d3-polygon computations). Each GeometryPart now caches `.orig` (WGS84), `.world` (z16), and `.local` (relative to origin). Updated `updateWorld()` to translate world coords to local space, compute hull/centroid/poi/ssr/area in local space for numerical stability, then translate results back to world. Enables future PixiGeometryPart removal by allowing Pixi containers to render local coordinate arrays directly with container transforms. Added comprehensive tests for `.local` property, all 2,951 tests pass.
- **Native z16 world-coordinate migration completed** (May 2026) — Rapid now uses SDK-native z16 world coordinates end-to-end. Removed `worldScaled` bridge types/data, deleted `modules/lib/worldScaled.ts`, updated SpatialSystem/Pixi paths to `world` (z16), removed Mapillary spike toggles, and validated with build/typecheck/tests.
- **NsiService refactor: NSI v7 types + dead code removal + complexity reduction** (Apr 2026) — Imported rich types from name-suggestion-index v7 (`OsmTags`, `NsiItem`, `NsiData`, `NsiDissolved`, `NsiJSON`, `NsiPath`, `DissolvedMap`, `NsiReplacementsJSON`, `MatchHit`, `NsiTree`, `NsiTreesJSON`). Removed dead wikipedia-replacement code path (NSI no longer tracks `wikipedia` in `replacements.json`). Converted 5 `forEach` calls to `for..of`. Extracted `_applyWikidataReplacements`, `_selectMatchedItem`, `_applyBranchSplit` helpers from `upgradeTags` to reduce cyclomatic complexity from 62 to ~22.
- **Tooling hardening + NetworkSystem test coverage** (Apr 2026) — `validate_json.ts` now exits nonzero on schema failures, `build_data.ts` no longer suppresses shell command failures, `server.ts` uses `process.cwd()` instead of shelling out for `pwd`, `API.md` uses the correct `.org` URL example, and `NetworkSystem.test.js` now covers request interceptors plus main-thread/worker listener dispatch branches.
- **MVT protobuf parsing on worker** (Apr 2026) — Added `network:fetchAndParseMVT` listener to `NetworkSystem.worker.ts`. Ships `@mapbox/vector-tile` + `pbf` to the worker bundle (~13KB). Fetches URL, decodes protobuf, converts features to GeoJSON via `toGeoJSON(x,y,z)`, returns `MVTFeatureResult[]`. VectorTileService standard MVT path and MapillaryService tile loading now use this listener. PMTiles path unchanged (still decodes on main thread). VectorTileService refactored: `_parseTileBuffer` (PMTiles-only) delegates to new `_processVTResults` (shared). MapillaryService: `_loadTile` (renamed from `_loadTileAsync`) and `_gotTile` simplified to process pre-parsed features.
- **Route all fetch calls through NetworkSystem** (Apr 2026) — Converted last non-service direct `fetch()` call sites: `AssetSystem` (removed hand-rolled `_inflight` map), `ImagerySourceEsri`, `PixiLayerCustomData`, `UiDefs` (uses `fetchRaw` for DOM parsing). Fixed relative URL resolution in workers via `NetworkSystem._resolveURL()`. Fixed `workerURL` timing — must be set in `prepareAsync().then()` BEFORE `initAsync`. All network calls now worker-offloaded by default.
- **Deferred worker result resolution** (Apr 2026) — Added `resultPriority` option to `WorkerSystem.dispatch()`. When set (`'urgent'`/`'normal'`/`'idle'`), the returned promise resolves through SchedulerSystem instead of immediately in `onmessage`. Prevents heavy `.then()` chains from blowing frame budgets. Wired through `NetworkFetchOptions.resultPriority` → `_dispatchFetch()` → `worker.dispatch()`. OsmService tile loading (`loadFromAPI`) uses `resultPriority: 'normal'`. Falls back to immediate resolution when SchedulerSystem is absent (tests, CLI). New `DispatchOptions` interface exported from WorkerSystem.
- **OsmService worker-based fetch+parse** (Apr 2026) — Created `OsmService.worker.ts` with `fetchAndParse` + `reset` listeners. OsmService `loadFromAPI` dispatches to worker via `listenerID: 'osmService:fetchAndParse'` with result envelope pattern (`OsmFetchResult`). Parser instances (`OsmJSONParser`/`OsmXMLParser`) live on the worker, not main thread. Reset simplified to call imported listener directly. 2950 tests pass, tsc clean.
- **Canvas-backed atlas textures** (Apr 2026) — Added optional canvas-backing for `AtlasAllocator` to support Pixi.js v8.16+ experimental canvas renderer. Root cause: canvas renderer reads `TextureSource.resource` directly via `canvasUtils.getCanvasSource()` and has no upload pipeline like GL/GPU. Fix: when `useCanvas: true`, `AtlasSource` creates a backing `HTMLCanvasElement` as the resource and blits each atlas item at allocation time. `PixiTextures.reset()` detects `RendererType.CANVAS` and passes `useCanvas: true`.
- **Experimental canvas renderer URL flag** (Apr 2026) — `renderer=canvas` URL param selects the Pixi canvas renderer (no WebGL/WebGPU). Added in `GraphicsSystem._initPixi()`. Known limitation: no Mesh support. Requires Pixi.js v8.16.0+.
- **Request interceptor API + OsmService migration** (Apr 2026) — Added `RequestInterceptor` type and `addRequestInterceptor` / `removeRequestInterceptor` API to NetworkSystem. Interceptors run on main thread before dispatch, produce serializable `RequestInit` for workers. OsmService migrated from `fetchFn: this._oauth.fetch` to `_authInterceptor` using `oauth.getAccessToken()` (osm-auth v3.2.0). Write operations keep `mainThread: true`.
- **xmldom 0.9.x migration** (Apr 2026) — Two breaking changes: (1) xmldom owns its own `Document` type — fixed with `import type { Document as XmlDocument }`. (2) Throws `ParseError` if `<?xml` not at position 0 — fixed with `.trimStart()` at all three XML-parsing sites.
- **Worker companion convention** (Mar 2026) — Established `*.worker.ts` companion file convention. `NetworkSystem.worker.ts` provides core listeners. `_dispatchFetch` routes named tasks (`listenerID` + `listenerData`) to WorkerSystem or calls listener directly as fallback.
- **WorkerSystem extraction** (Mar 2026) — Extracted worker pool + listener registry from SchedulerSystem and NetworkSystem into dedicated `WorkerSystem`. Workers are long-lived (resetAsync does NOT terminate them). NetworkSystem changed optional dependency from `'scheduler'` to `'worker'`.
- **NetworkSystem service migration — COMPLETE** (Mar 2026) — All services migrated to NetworkSystem. OsmService was last: removed 3 redundant inflight caches, added `hasMatching()`, fixed copy-paste bug in note POST handlers, renamed `osm-note-` → `osm-note-tile-`.
- **NetworkSystem Phase 6b** (Mar 2026) — All 10 Easy+Medium tier services migrated. `key` → `requestID` with `RequestID` global string ID type. `abortMatching` predicates use regex `.test()`. 2939 tests pass.
- **NetworkSystem Phase 6a** (Mar 2026) — `NetworkSystem` centralizes fetch lifecycle: inflight tracking, dedup, timeout, WorkerSystem.dispatch offloading, concurrency limiting. Committed as `74a291f18`.
- **SchedulerSystem Phase 5: Backpressure** (Mar 2026) — EMA frame metrics, dropped-frame ring buffer, pressure levels (none/light/moderate/heavy) with hysteresis, idle queue throttling. 117 tests.
- **SchedulerSystem Phase 4a** (Mar 2026) — Migrated all lodash debounce/throttle (22 sites, 16 files) to SchedulerSystem workID-keyed API.
- **SchedulerSystem Phase 4** (Mar 2026) — workID-keyed timer API: `setTimeout`, `setInterval`, `debounce`, `throttle`. `cancel(workID)` / `cancelAllTimers()`.
- **SchedulerSystem Phase 3** (Mar 2026) — Priority queues (urgent/normal/idle) drained per-frame within budget. New `schedule(fn, opts)` API. `targetFrameTime` property.
- **SchedulerSystem Phase 2** (Mar 2026) — SchedulerSystem owns the rAF game loop. GraphicsSystem registers `_tick(deltaMS)` as a frame callback. PIXI.Ticker removed.
- **SchedulerSystem Phase 1** (Mar 2026) — Foundation: `scheduleIdleTask`, `scheduleTimeout`, `scheduleInterval`. All `requestIdleCallback` usage migrated.
- **Validators TS conversion + JSDoc** (Mar 2026) — All 19 validator files converted. `ValidatorResult` is now `{ issues, provisional? }`. Entity narrowing pattern applied.
- **Validators folder rename** (Mar 2026) — `modules/validations/` → `modules/validators/`, `duplicate_way_segments` → `duplicate_segments`.
- **Data class renames** (Mar 2026) — `Tags` → `OsmTags`, `GeoJSON` → `GeoJSONData`, `Marker` → `MarkerData`.
- **Context lifecycle split** (Mar 2026) — `prepareAsync()` → `initAsync()` → `startAsync()` → `run()`.
- **JSON Schema validation** (Mar 2026) — 13 schemas in `data/schema/`, validated via `bun run validate:json`.
- **Scope-owned tag rulesets** (Mar 2026) — All tag globals eliminated. Rulesets on `SchemaScope`. `tags.ts` deleted.
- **Variables system** (Mar 2026) — `Variable` class with `var()` refs in SchemaSystem and StyleSystem. Dual-props pattern for resolution.
- **Scoped customization** (Feb 2026) — `_scopes: Map<ScopeID, ScopeData>` as sole source of truth in StyleSystem and SchemaSystem. `'*'` common scope for fallbacks.
- **Dynamic styling fixes** (Feb 2026) — `dirtyScene()` before redraw, removed hardcoded marker styles, single `styleDefaults`.
- **Services TS conversion** (Feb 2026) — All 19 service files converted. Generic `MarkerData<P>` / `GeoJSONData<P>` for typed props.
